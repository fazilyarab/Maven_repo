pipeline {
    agent any
    stages {
        stage('new Application') {
            steps {
                 pom: 'Maven_repo/pom.xml',
                    goals: 'clean install',
                sh  'mvn --version' 
            }
            
            }
            }
            }
